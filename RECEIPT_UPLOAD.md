# 📸 ระบบอัปโหลดรูปใบเสร็จลง Airtable

## ✅ ระบบพร้อมใช้งานแล้ว!

ระบบสามารถบันทึกรูปใบเสร็จลง Airtable ได้แล้ว

---

## 🔄 วิธีทำงาน

### ขั้นตอนที่ 1: ผู้ใช้เลือกไฟล์ใบเสร็จ
- ผู้ใช้คลิก "เลือกไฟล์" และเลือกรูปใบเสร็จ
- ระบบแสดงตัวอย่างรูปภาพ

### ขั้นตอนที่ 2: อัปโหลดไปยัง Imgur
- เมื่อผู้ใช้กด "บันทึกข้อมูล"
- ระบบแปลงไฟล์เป็น base64
- อัปโหลดไปยัง Imgur ผ่าน API `/api/upload`
- ได้ URL ของรูปภาพกลับมา

### ขั้นตอนที่ 3: บันทึกลง Airtable
- ระบบส่งข้อมูลการจอง + URL ของรูปใบเสร็จไปยัง API `/api/bookings`
- Airtable บันทึกข้อมูลพร้อม Receipt attachment
- รูปใบเสร็จจะแสดงใน Airtable Table

---

## 📋 ข้อมูลที่บันทึกใน Airtable

เมื่อบันทึกสำเร็จ จะมีข้อมูลดังนี้:

| Field | ข้อมูล |
|-------|-------|
| First Name | ชื่อที่กรอก |
| Last Name | นามสกุลที่กรอก |
| Time Slot | ช่วงเวลาที่เลือก |
| Room ID | ID ของห้อง |
| Room Name | ชื่อห้อง |
| **Receipt** | **รูปใบเสร็จ (Attachment)** ✅ |
| Status | Pending |
| Created At | วันที่และเวลาที่บันทึก |

---

## ⚙️ ข้อกำหนด

### 1. ต้องตั้งค่า Imgur Client ID

ดูคู่มือใน `IMGUR_SETUP.md` สำหรับการตั้งค่า

### 2. Field "Receipt" ใน Airtable

- Type: **Attachment**
- ต้องมี Field นี้ใน Table "Bookings"

---

## 🧪 ทดสอบการทำงาน

### 1. ตรวจสอบว่า Imgur Client ID ตั้งค่าแล้ว

```bash
# ตรวจสอบ .env.local
cat .env.local | grep IMGUR_CLIENT_ID
```

### 2. รันแอป

```bash
pnpm run dev
```

### 3. ทดสอบอัปโหลด

1. เปิดเบราว์เซอร์ไปที่ http://localhost:3000
2. เลือกช่วงเวลาและห้อง
3. กรอกชื่อ-นามสกุล
4. **เลือกไฟล์ใบเสร็จ** (สำคัญ!)
5. กด "บันทึกข้อมูล"
6. ตรวจสอบใน Airtable ว่ามี Receipt attachment หรือไม่

---

## 🔍 ตรวจสอบใน Airtable

1. ไปที่ Airtable: https://airtable.com
2. เปิด Base "Queue Master"
3. เปิด Table "Bookings"
4. ดู Record ที่เพิ่งสร้าง
5. ตรวจสอบ Field "Receipt" ว่ามีรูปภาพหรือไม่

---

## ⚠️ ข้อควรระวัง

### ถ้าอัปโหลดรูปไม่สำเร็จ

- ระบบจะแสดงคำเตือน แต่จะบันทึกข้อมูลการจองไว้
- ข้อมูลการจองจะถูกบันทึก แต่ไม่มี Receipt attachment
- ตรวจสอบ Console log เพื่อดู error message

### ข้อจำกัดของ Imgur

- ไฟล์สูงสุด 10MB
- 1,250 uploads ต่อวัน (Free tier)
- รูปภาพจะเป็น public (ใครก็ได้ที่รู้ URL สามารถดูได้)

---

## 🐛 แก้ไขปัญหา

### ปัญหา: "Imgur Client ID not configured"

**วิธีแก้:**
1. ตรวจสอบว่าเพิ่ม `IMGUR_CLIENT_ID` ใน `.env.local` แล้ว
2. Restart development server

### ปัญหา: รูปไม่แสดงใน Airtable

**วิธีแก้:**
1. ตรวจสอบว่า Field "Receipt" เป็น Type "Attachment"
2. ตรวจสอบ Console log ว่ามี error หรือไม่
3. ตรวจสอบว่า URL จาก Imgur ถูกต้องหรือไม่

### ปัญหา: อัปโหลดล้มเหลว

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์ไม่เกิน 10MB
2. ตรวจสอบว่าเป็นไฟล์รูปภาพ (jpg, png, gif, etc.)
3. ตรวจสอบ Imgur API status

---

## 📊 Flow Diagram

```
ผู้ใช้เลือกไฟล์
    ↓
แปลงเป็น base64
    ↓
อัปโหลดไปยัง Imgur
    ↓
ได้ URL กลับมา
    ↓
ส่ง URL + ข้อมูลการจองไปยัง Airtable
    ↓
Airtable บันทึกข้อมูล + Receipt attachment
    ↓
สำเร็จ! ✅
```

---

## ✅ Checklist

- [ ] ตั้งค่า Imgur Client ID แล้ว
- [ ] Field "Receipt" ใน Airtable เป็น Type "Attachment"
- [ ] ทดสอบอัปโหลดรูปใบเสร็จ
- [ ] ตรวจสอบใน Airtable ว่ามี Receipt attachment

---

## 🎉 พร้อมใช้งาน!

ระบบพร้อมใช้งานแล้ว เมื่อผู้ใช้ส่งรูปใบเสร็จมา ระบบจะ:
1. ✅ อัปโหลดไปยัง Imgur
2. ✅ บันทึกลง Airtable พร้อม Receipt attachment
3. ✅ แสดงรูปใบเสร็จใน Airtable Table

